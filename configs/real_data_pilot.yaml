name: grigoreva_real_data_pilot
features:
  market_average: true
  rolling: true
  rolling_window: 1000
model:
  architecture: gru3
  auxiliary_targets: true
training:
  epochs: 1
  learning_rate: 0.0005
  device: cuda
online:
  enabled: false
  learning_rate: 0.0003
ensemble:
  architectures: [gru3]
  seed_ensembling: false
  seeds: [0]
cv:
  min_date: 700
  n_splits: 1
  validation_days: 3
  gap_days: 2
